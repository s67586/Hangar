#!/usr/bin/env bash
#
# 假的 nc。lan backend 用 `nc -z -w 2 <ip> <port>` 測 5555 通不通，
# 測試不該真的對外連線。由 $MOCK_STATE/nc_result 控制：ok（預設）= 通。
#
S="$MOCK_STATE"
echo "nc $*" >> "$S/nc_log"
[ "$(cat "$S/nc_result" 2>/dev/null || echo ok)" = "ok" ]
